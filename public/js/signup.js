const signUpFormHandler = async (event) => {
    const userNameEl = document.getElementById('user-name');
    const passwordEl = document.getElementById('pass-word');
    const confirmPasswordEl = document.getElementById('confirm-password');

    if 
    
}
