<div class="row">
    <div class="col-auto">
        {{#if logged_in}}
        <h2>Welcome, {{user.username}}!</h2>
        {{/if}}
    </div>
</div>
<br>
{{#if posts.length}}
<div class="card flex-column">
    {{#each posts as |post|}}
    {{>editItem post}}
    {{/each}}
</div>
{{/if}}