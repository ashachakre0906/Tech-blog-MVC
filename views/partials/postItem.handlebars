<div class="card-body">
  <a href="/post/{{id}}">
        <div class="card-header">
      <div class="col-8">{{post_title}}</div>
      <div class="col-4">
        by:{{user.username}}
        on{{format_date date_created}}</div>
    </div>
  </a>
  <p class="card-body">{{post_description}}</p>
</div>